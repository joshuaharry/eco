#!/bin/sh
# TODO: Write a script that downloads the Go compiler and builds our project
# with it to minimize setup costs. We should also write a version of the script
# that works on Windows with a batch file, too.
